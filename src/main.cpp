#include <trygonometria.h>

int main() { 

	double sinus = degreemath::sin(30.0);
	double cosinus = degreemath::cos(30.0);
	double tangens = degreemath::tg(30.0);
	double cotangens = degreemath::ctg(30.0);

	return 0; 
}
